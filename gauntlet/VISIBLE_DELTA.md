# Visible delta — Forge Ink Phase B2 (Night Press locked)

**Rule:** Pass fails if previous live demo cannot be distinguished in ~3 seconds.  
**Identity:** Still **Night Press** (no reseed).  
**Anti-slop:** Flat chrome — no decorative gradients / glass blur / glow.

## Screenshots
| | Path |
| --- | --- |
| **Before** (B1 live gallery) | `gauntlet/shots/b2-before-gallery.png` |
| **After** (R2 gallery) | `gauntlet/shots/b2-r10-gallery.png` |
| **After** (editor + export dock) | `gauntlet/shots/b2-r10-editor.png` |

## Five visible deltas (spot in <3s)
1. **Job tape** — Full-width lime strip: `Compose graphic → Fixed size → Export SVG / PDF` (absent before).
2. **Materials tape** — Dark strip under brand: `Night Press / Fixed size / Local ink / SVG + PDF`.
3. **Gallery billboard** — H2 **Compose. Export.** + seven real shop templates with size badges and headline plates (was five abstract cards).
4. **Export dock** — Bottom-right `Ready to pull` + **Export PDF** after template load (new).
5. **Flat chrome** — Paper well / cards / dock use flat fills + hard offsets; gradient washes, glass blur, and glow removed.

## Not counted
IDENTITY.md / docs-only / +3% opacity — excluded per visibility gate.
